var names = ["Saud ", "Naveed", "Hussain", "Abrar"];
// Print each person's name one at a time
names.forEach(function (name, index) {
    console.log("Friend ".concat(index + 1, ": ").concat(name));
});
